<h1>All our tasks</h1>

<ul>
<% @tasks.each do |task| %>
  <li>
    <%= task.title %>
    <%= task.details%>
    <%= task.completed %>
    <%= link_to "Show", task_path(task) %>
  </li>
<% end %>
</ul>
<%= link_to "add new task", new_path%>


